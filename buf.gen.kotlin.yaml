# The runnable Buf template is generated by :protocol:writeProtocolGenerationConfig.
# Gradle resolves pinned local protoc and Connect-Kotlin generator artifacts and
# writes their platform-specific paths to protocol/build/protocol-tools.
#
# Use ./gradlew generateProtocol instead of invoking buf generate directly.
